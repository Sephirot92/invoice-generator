version: 2.1

jobs:
  build:
    docker:
      - image: 'circleci/openjdk:11-jdk'
    steps:
      - checkout
      - run:
          name: Analyze on SonarCloud
          command: gradle build sonarqube -Dsonar.projectKey="SilverSabre92_invoice-generator" -Dsonar.organization="silversabre92" -Dsonar.host.url="https://sonarcloud.io"

workflows:
  main:
    jobs:
      - build:
          context: SonarCloud